"use client";
import {useGSAP} from "@gsap/react";import gsap from "gsap";import {ScrollTrigger} from "gsap/ScrollTrigger";import {useRef} from "react";
gsap.registerPlugin(ScrollTrigger);
export default function HeroAnimations(){const root=useRef<HTMLDivElement>(null);useGSAP(()=>{const ctx=gsap.context(()=>{gsap.from(".hero-kicker",{y:18,opacity:0,duration:.7,ease:"power3.out"});gsap.from(".hero-title",{y:70,opacity:0,duration:1,delay:.15,ease:"power4.out"});gsap.from(".hero-copy",{y:25,opacity:0,duration:.7,delay:.35});gsap.from(".hero-actions",{y:20,opacity:0,duration:.7,delay:.45});gsap.from(".hero-orb",{scale:.5,opacity:0,duration:1.2});gsap.to(".hero-orb",{y:-28,x:12,duration:4,repeat:-1,yoyo:true,ease:"sine.inOut"});gsap.utils.toArray<HTMLElement>(".reveal").forEach(el=>gsap.from(el,{y:40,opacity:0,duration:.8,scrollTrigger:{trigger:el,start:"top 85%"}}));gsap.to(".progress-line",{width:"100%",duration:1.2,scrollTrigger:{trigger:".progress-wrap",start:"top 80%"}})},root);return()=>ctx.revert()}, {scope:root});return <div ref={root} className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true"><div className="hero-orb absolute -right-24 top-24 h-72 w-72 rounded-full bg-white/10 blur-3xl"/><div className="absolute left-1/2 top-1/3 h-px w-1/2 -translate-x-1/2 bg-gradient-to-r from-transparent via-white/20 to-transparent"/></div>}
